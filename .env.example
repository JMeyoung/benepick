# SQLite 데이터베이스 파일 경로 (배포 시 PostgreSQL 연결 문자열로 교체)
DATABASE_URL="file:./prisma/dev.db"

# 관리자 세션 토큰 서명 키 — `openssl rand -hex 32`로 생성
SESSION_SECRET="replace-with-random-64-hex"

# 시드로 생성되는 관리자 계정
ADMIN_EMAIL="admin@benepick.local"
ADMIN_PASSWORD="benepick2026!"

# PostHog 분석 (선택 — 없으면 추적 비활성화)
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# 쿠콘(Coocon) 카드 혜택 API (유료 — coocon.net에서 B2B 계약 후 발급)
COOCON_API_KEY=""
COOCON_API_URL=""

# 온통청년 Open API (무료 — youthcenter.go.kr 마이페이지 → OPEN API에서 발급)
YOUTH_CENTER_API_KEY=""

# 보조금24 공공서비스 API (무료 — data.go.kr에서 활용신청 후 발급)
GOV_DATA_API_KEY=""

# Wello 정책 API (유료 — welloapi.com에서 계약 후 발급)
WELLO_API_KEY=""
WELLO_API_URL=""
